import 'package:flutter/material.dart';

//INFO: don't change the order of the priority
const List<String> priorityLabels = ["Low", "Moderate", "High", "Critical"];

// Define colors for each priority level
const List<Color> priorityColors = [
  Colors.green,
  Color.fromARGB(255, 241, 206, 2),
  Colors.orange,
  Colors.red,
];
